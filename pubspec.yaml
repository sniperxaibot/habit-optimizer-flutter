name: habit_optimizer
description: "AI Daily Habit Optimizer with Grok AI"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  provider: ^6.1.2
  flutter_local_notifications: ^17.2.0
  http: ^1.2.2
  shared_preferences: ^2.3.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/